<?xml version="1.0" encoding="utf-8"?>
<!--
  Meelano VPN — app manifest.

  Four things in here exist because of the two bugs this project is fixing (app killed on close +
  a multi-second hang right after connect), so they are commented with the reason, not the flag:

   1. `MeelanoVpnService` is a bound+started foreground service with `specialUse`. The system keeps
      a service alive when it has an active notification channel that is declared as the VPN type —
      that, not a foreground-service "hack", is what stops "closing the app = everything dies".
   2. No battery-optimisation exemption is requested silently; the launcher activity asks once
      (onboarding step 2) and `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` is only used with the user's
      tap, otherwise Play rejects the release.
   3. `usesCleartextTraffic=false` + a network security config: the update channel must be HTTPS and
      the feed host is the only exception, because some of you still have a shared host without TLS.
   4. `android:launchMode=singleTask` + `stateNotNeeded=false`: re-launching after a kill must reuse
      the running service instead of starting a second connect (a second connect = the freeze).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- self-update: the system only allows this after the user grants it in the installer prompt -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Android 11+ needs this declared before we can launch the "install unknown apps" screen -->
    <queries>
        <intent>
            <action android:name="android.settings.MANAGE_UNKNOWN_APP_SOURCES" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="package" />
        </intent>
    </queries>

    <application
        android:name=".MeelanoApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meelano"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout|smallestScreenSize|uiMode|density|layoutDirection"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- import a config that a browser/QR app hands over; never auto-connects (spec §13) -->
            <intent-filter android:label="@string/app_short">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vless" />
                <data android:scheme="vmess" />
                <data android:scheme="trojan" />
                <data android:scheme="ss" />
                <data android:scheme="hysteria2" />
                <data android:scheme="meelano" />
            </intent-filter>
        </activity>

        <service
            android:name=".vpn.MeelanoVpnService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService.SERVICE" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="tunnel" />
        </service>

        <service
            android:name=".qs.MeelanoTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_vpn"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- install the downloaded APK: FileProvider URI, because file:// is rejected since N -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- only reconnect if the user was connected; never a "keep-alive pinger" -->
        <receiver
            android:name=".keepalive.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" tools:ignore="BatteryLevel" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="android.app.shortcuts"
            android:resource="@xml/shortcuts" />
    </application>
</manifest>
