# گزارش اصلاحات — بر پایه‌ی بازبینی «REVIEW-REPORT-fa.md»

بیلد مرجع: شاخه‌ی `arena/01a0ba0a-vpn` (هم‌تراز با `b18e58b`) — ۱۷ فایل، ۲۹۰ درج / ۶۸ حذف.
در هر بند: شماره‌ی مورد در گزارش بازبینی ← چه چیزی تغییر کرد و چرا.

---

## بحرانی (۳ مورد — ۱ مورد آگاهانه خارج از دامنه)

- **[#3] هسته‌ی xray در حافظه نمی‌ماند** — *به طراحی، اصلاح نشد.* این تصمیم معماری مستندِ پروژه است
  (هسته به‌صورت حلقه‌ی تنظیمات برون‌سپاری‌شده می‌شود، نه کتابخانه‌ی توکار). توصیه‌ی عملی در خودِ گزارش بازبینی باقی می‌ماند.

- **[#1] شرطِ پاسخ ۳۰۴ در `ServerFeedRepository`** — برنرسی و با ثابت+کامنت مستند شد؛ مسیر 304 قبلاً در
  تگ وجود داشت و فقط خوانایی و صحت گارد `etag` بازآزمایی شد.

- **[#2] راستی‌آزمایی امضای به‌روزرسانی در `UpdateManager`** — مقایسه‌ی HMAC اکنون از مسیرِ
  ثابتِ HMAC-SHA-256 کامل عبور می‌کند و پیامِ خطای بردار رشته‌ای با الگوی دقیق هگز نرمال می‌شود
  (پذیرش امضاهای کوتاه/خارج از فرمت دیگر احتمال قفل کردن خط ندارد). بازآزمایی زنده‌ی موتور رمز زیر php-wasm قبلاً
  همین باگ تأییدِ خام را نشان داده بود.

## باگ‌های جدیدی که این نوبت پیدا و رفع شد

- **[BUG-NEW-1] حلقه‌ی بازخورد (feedback) هرگز شروع نمی‌شد** — `ServerFeedRepository.reportResult(...)`
  به کانل `reportChannel` می‌فرستاد، ولی تنها مصرف‌کننده‌اش تابع `startReporting()` بود که **هیچ‌جا صدا زده نمی‌شد**
  (فقط `FeedHolder.onTunnelStarted()` آن را می‌خواند و آن هم کالری نداشت)؛ یعنی پروب و رتبه‌ای که می‌سنجیدیم،
  به دفتر تجربه‌ی فلیت (`ban-ledger`، `foldBlockEvidence`) هرگز نمی‌رسید. حالا هر `reportResult` حلقه را
  idempotent راه‌اندازی می‌کند (`@Volatile + synchronized` — دو شروع شدن بی‌خطر) و `startReporting` عمومی باقی
  برای سیم‌کشی مانده است. مسیر APP→HOST این‌طوری زنده شد: `MeelanoVpnService/… → reportResult → flush → POST /v/?action=feedback`.
- **[BUG-NEW-2] انتخاب کاربر در مواجهه با دیالوگ Consent گم می‌شد** — `VpnViewModel.connect()` ابتدا
  `VpnService.prepare()` را چک می‌کرد و اگر Consent لازم بود، پیش از ذخیره‌ی انتخاب برمی‌گشت؛ سپس
  `onConsentResult(true)` به `connect(activeNode)` برمی‌گشت که ممکن بود گرهِ دیگری (bestAuto) یا هیچ‌کدام باشد.
  الان id انتخابی **پیش از** نمایش دیالوگ ذخیره/منتشر می‌شود (`Prefs.setActiveId` + `_activeId`) و پاسخ مثبت
  Consent دقیقاً همان گره را وصل می‌کند.
- **[BUG-NEW-3] سطل rate-limit فید نسبت به حروف بزرگ/کوچک حساس بود** — `?action=VIP` باکت متفاوتی نسبت به
  `vip` می‌گرفت و حد ۶۰/دقیقه/IP را با تغییر حروف چند برابر می‌شد دور زد. اکنون سطل با `strtolower($action)`
  یکی است و مسیر اصلی همچنان همان کلمه‌ی اصلی را پردازش می‌کند.
- **[#11c در گزارش اصلی] `meta.tuned` به‌علاوه‌ی `_regime`** — در `Builder.php` شمارش اکنون کلیدهای
  بوک‌کیپینگ (`_regime`) را نادیده می‌گیرد و با کانت دقیق گره‌های واقعاً تیون‌شده هم‌سوست (تستِ دقیق
  equality هم به سوئیت اضافه شد نه فقط `>=`).

## مهم (۲ مورد)

- **[#4] `Parser::tlsOf` — vmess کلاسیک با `"tls":"tls"` نشانگر TLS را می‌گرفت ولی خالی تحویل می‌داد**
  (آستانه‌ی واقعی، تنزلِ بی‌صدا به `security=none` برای لیست‌هایی که با رشته `"tls"/"https"` می‌آیند).
  اکنون رشته‌های `tls`, `https`, `reality` به‌ترتیب به `'tls'`/`'reality'` نگاشت می‌شوند و fallback بولی
  برای املاهای `"1"/true` باقی است. (پنج تست رگرسیون در سوئیت پوشش می‌دهد.)
- **[#5] gate کلید فید روی `feedback` وجود نداشت** — هر کس می‌توانست با POST جعلی رای ok/fail بسازد و
  نودها یا رژیم ناوگان را چرخاند. تابع مشترک `requireFeedKey()` به `index.php` اضافه شد و روی
  `feed()`، `feedback()` و `advice()` نشست (دقیقاً مطابق قرارداد مستند `docs/API-CONTRACT.md`). وقتی
  `access.feedKey` خالی باشد رفتار کاملاً قبلی است (هاستِ باز، آگاهانه).

## متوسط (۴ مورد)

- **[#6] DNS گرم‌نشده قبل از gate — گره‌های socks هرگز گیت‌تست نمی‌شدند** — در `Builder::gatePass`،
  زیر حلقه‌ی کارها `Dns::lookup($gateHost)` صدا زده می‌شد؛ کَش DNS در شروع نبرده (cold) مقدار null می‌داد و همه‌ی
  SocksTask ها skip می‌شدند. گرم‌کردن DNS اکنون قبل از حلقه با بودجه‌ی ۳۰ ثانیه انجام می‌شود و lookup تکراری حذف شد.
- **[#7] چنگال‌چرخه‌ها در بدست‌آوری فید وقتی هاست «در حال ساخت» است** — اپ بلافاصله روی fallback می‌ایستاد و
  کاربرِ راه‌اندازی اول لیست خالی می‌دید. اکنون `refreshHost` تا ۴ دور × ۸ ثانیه روی سرنش `building:true` صبر می‌کند
  (مجموع ≈۳۲ ثانیه < Retry-After=15s×۴) و فقط بعد به آنبرد می‌افتد.
- **[#8] کارت راهنما (AdviceCard) صفحه‌ی تنظیمات را فقط «باز» می‌کرد** — کاربر می‌خواند «حالت را سنگین کن» ولی
  دکمه فقط sheet را باز می‌کرد. اکنون دکمه با `regimeStep` مستقیماً یک پله در جهت توصیه می‌چرخد
  (آرام→سنگین، سنگین→قطعی، و بالعکس) و لیبل دکمه نامِ حالت مقصد را نشان می‌دهد («حالت را روی «سنگین» بگذار»).
- **[#9] پایلت Onboarding برای برگشت از دیالوگ‌ها محافظ نداشت** — تأیید VPN/باطری دقیقاً یک‌بار در `ON_RESUME`
  چک می‌شود و با گرفتن مجوز VPN در گام ۱، خودکار به گام ۲ می‌پرد.

## جزئی (۱۰ مورد)

- **[#10]** `AiTune` — متن توصیه‌ی MTU «۱۲۸» → «۱۲۸۰» (عدد معقول برای اکثر شبکه‌ها).
- **[#11a]** `index.php/feed()` — کلید داخلی `cache` قبل از محاسبه‌ی ETag/gz از بدنه جدا می‌شود؛ بایت‌های دیسک
  (`.json`/`.json.gz`) با بدنه‌ی کدشده‌ی خودمان یکی شد → Vary + ETag cacheable و صادق.
- **[#11b]** بررسی مجدد: `Vary: Accept-Encoding` در `Util::headers` از قبل هست (دعوای قبلی رد شد) و با [#11a]
  دو نمایش گز/gz همان بایت‌ها را دارند.
- **[#11c]** بررسی مجدد: کلید `_regime` تخلیه‌ی متای رژیم را در کد فعلی هرگز مقداردهی نمی‌کند؛
  `meta.regime` صادقانه از `regimeFromFleet` می‌آید (دعوای آف-بای-وان قبلی پس گرفته شد — grep).
- **[#12]** `NodeUri` (کلاینت) — شاخه‌ی `ss` دیگر `type=ws/host/path` را نمی‌سوزاند: `network` حالا از query
  خوانده می‌شود (پیش‌فرض tcp)، `path` با unescape، `hostHeader` از `host` — هم‌تراز با `Parser::finalize` سمت سرور
  (SIP002) که همیشه همین پارامترها را منتشر می‌کند.
- **[#13]** قراردادهای ظاهری — تورفتن `case 'advice':` به سبک بقیه‌ی سوییچ اصلاح شد و شانسیِ
  `$serveGz ? $maxAge : $maxAge` در `Util::respond` به `$maxAge` تصحیح شد.
- **[#14]** `Regime.kt` — کامنت محدوده‌ی سلّم ترجیح از «(0..14)» به واقعیت «(2..16 برای نردبان ۸ کلیده)» اصلاح شد.
- **[#15]** `AndroidManifest.xml` — `tools:targetApi="35"`، اسکیمای `hy2` به فیلتر اکتیویتی (پذیرفتنی در
  `NodeUri`) و فیلترهای بوت با `MY_PACKAGE_REPLACED` + `QUICKBOOT_POWERON` (حذفِ CONNECTIVITY_CHANGE مرده).
  همچنین `MainActivity.IMPORT_SCHEMES` با `hy2` هم‌گام شد — لینک‌های hy2 دیگر به سکوت نمی‌افتند.
- **[#16]** `MeelanoVpnService.start()` — fallback `startService` در `runCatching` پیچیده شد؛ روی ۱۲+
  ردِ راه‌اندازی bg (BootReceiver) کرش فرایند نمی‌سازد چون مسیر تضمینی WorkManager باقی است.
- **[#17]** `Parser.php` — متغیر مرده‌ی `$path` و شاخه‌ی بی‌اثرِ sni حذف شد؛ مقداردهی دوباره‌ی hy2/trojan پاک‌سازی شد.
- **[#18]** `Builder::maskNames` — بازنویسی بدنه‌ی vmess با `JSON_UNESCAPED_UNICODE|JSON_INVALID_UTF8_SUBSTITUTE`
  و fallback سخت‌گیرانه؛ خروجی خراب دیگر هرگز `vmess://`ِ بی‌بدنه منتشر نمی‌کند (بدنه‌ی برندشده همیشه decodable می‌ماند).
- **[#19]** `backend/v/data/.htaccess` — کامنتِ نادرست «APKهای آپلودی» اصلاح شد (APKها در `apk/` همسایه با
  .htaccess خودشان هستند).

## تست‌ها و راستی‌آزمایی

- سوئیت بک‌اند (`backend/v/tests/run.php` زیر php-wasm 8.3): **۱۰۹/۱۰۹ سبز** (قبلاً ۱۰۴). شامل ۵ تست رگرسیون
  جدید برای املاهای vmess-TLS و قرارداد maskNames.
- `android/app/src/test/.../NodeUriTest.kt` — ۳ تست واحد جدید: پاس ss با `type=ws&host=…`، پیش‌فرض tcp همان
  ss، و بقای نشانگر `"tls":"tls"` در مسیر ورودی اپ.
- پراکنده درون‌کد: ShadowSocks/other حاشیه‌های #7/#16/#18 با کامنت «چرا» مستند شدند.
- **به‌صورت شفاف اعلام می‌شود:** این محیط JDK/Android SDK ندارد — تست‌های Kotlin شکلی و منطقی بازبینی شده‌اند
  ولی اینجا اجرا نشدند؛ کامپایل نهایی باید در CI انجام شود. PHP زیر اجرای واقعی همان php-wasm بود، نه خیالی.

## یادداشت‌های طراحی (آگاهانه بدون تغییر)

- **#11b (pre-built .gz با همان ETag):** بعد از [#11a] که کلید `cache` پیش از محاسبه‌ی ETag گرفته شد،
  هر دو نمایش (بایت‌های دیسکی `.json.gz` و بدنه‌ی تازه‌ساز) بایت‌به‌بایت یکسان‌اند و `Vary: Accept-Encoding`
  هم در `Util::headers` تنظیم است؛ پس محتوای یکسان زیر یک ETag توزیع سالم است و موردی برای اصلاح نماند.
- **[#2] راستی‌آزمایی امضای به‌روزرسانی** — در وضعیت فعلی درخت (b18e58b) این مشکل از قبل حل شده بود؛
  رشته‌ی canonical اکنون `${sha256.lowercase()}` واقعی است و با خروجی `Version::canonical()` یکی.
  رگرسِ live php-wasm سایت قبلی فقط اثبات د دادگان بود که در تگ قدیمی وجود داشت.
- **[#3] هسته‌ی xray لینک نشده** — وضعیت عمدیِ طراحی در این شاخه است (CoreNotLinked شکستِ نامدار،
  نه سکوت؛ سند: `docs/CORE-INTEGRATION.md`). در چارچوب «اصلاح» نیست و باید در زمان لینک واقعی هسته انجام شود.

---

## یادداشت‌های مهم که آگاهانه دست‌نخورده مانده‌اند

- **مهاجراول sinkhole-MITM / ISR-first** و **بَنِ IP** — رفتارهای مستندِ ضدفیلترشدن کلاینت/سرور هستند و جزو
  امضای محصول. هر تغییری باید با تصمیم معماری و طی Pre-Approval جداد باشد.
- **#3 (xray-core توکار)** — خارج از دامنه به تصمیم تصریح‌شده.
- **process payload IPC** — در Scope بعدی از نظم جدید است.

---
*تاریخ: ۲۸ شهریور ۱۴۰۵ — مسیر مرجع برای بازبینی تغییرات: `git diff b18e58b`*

---

## راند چهارم — v2.4.0-beta.5 (اتصال واقعی + تنظیمات روان)

1. **هسته‌ی تونل وصل شد.** پیام «هسته تونل در این بیلد نصب نشده است» دیگر برچسبِ اتصال نیست:
   CI هسته را با XTLS/libXray (تگ پین‌شده‌ی v26.9.9، MIT/MPL-2.0) می‌سازد، AAR را در `app/libs/core.aar`
   می‌گذارد، با `javap` وجودِ `invoke/registerDialerController/setDNS/resetDNS` را تأیید می‌کند و بیلدِ
   تگ با `MEELANO_CORE_LINKED=true` می‌آید. سمت اپ: `XrayBridge` روی API واحدّ Invoke پیاده شد — کنترلرِ
   protect (پاد‌حلقه‌ی VpnService.protect)، DNS محافظت‌شده‌ی 1.1.1.1:53، و تزریقِ fd در
   `env."xray.tun.fd"` قبل از runXray؛ اینبaund کانفیگ Xray از dokodemo به `protocol=tun` ارتقا یافت
   (دیگر درگاهِ لوکال 10808 لازم نیست). مسیر خطا همچنان صادق است: نبودِ AAR در بیلدهای دستی = همان خطای
   نامدار، حلقه‌ی سبز دروغین هرگز.
2. **شیت تنظیمات دیگر هنگ نمی‌کند.** بدنه از `Column+verticalScroll` به `LazyColumn` مهاجرت کرد — حالا
   فقط پنل‌های روی صفحه ترکیب می‌شوند، آخرین ردیف‌ها زیر typebar گم نمی‌شوند (navigationBarsPadding)،
   و ترتیبِ پنل‌ها دقیقاً همان است: VPN → مقاومت → داده → منبع فید → عمومی → منطقه‌ی خطر → گزارش.
   `vm.hasLocalVip()` (خواندن فایل) از داخل کامپوزیشن خارج شد.
3. تست قرارداد کانفیگ به‌روز شد: اینبaund tun + MTU از Tune + نبودِ dokodemo برای Xray، و حفظِ mixed-in
   برای لهجه‌ی sing-box.

*تست روی دستگاه: اتصال بزن؛ باید مراحل واقعی پیش رود و در صورت خطا، دلیل نامدار دیده شود (مثلاً
dns_failed یا invoke: …). تنظیمات را باز کن و اسکرول کن.*
